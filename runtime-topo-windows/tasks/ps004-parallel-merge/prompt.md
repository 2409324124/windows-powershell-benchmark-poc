Fix `build.ps1` so it runs under Windows PowerShell 5.1 and processes all six input chunks concurrently with a hard maximum of three workers. `ForEach-Object -Parallel` and PowerShell 7-only syntax are unavailable. Each worker must receive exact input, output, and state paths. Wait for every worker, fail the build if any worker fails, and create `out\aggregate.txt` in input filename order even though workers finish out of order. Do not fabricate worker state or serialize the work.
