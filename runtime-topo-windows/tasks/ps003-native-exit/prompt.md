Fix `pipeline.ps1` for Windows PowerShell 5.1. Run the analyzer with the input path preserved as one argument, capture stdout and stderr into separate files named `out\stdout.txt` and `out\stderr.txt`, and record the analyzer's actual native exit code in `out\analyzer-exit.txt` immediately after it exits. If the analyzer fails, do not invoke the publisher and return the same native exit code from the script. Do not suppress diagnostics, merge the streams, or hard-code the expected analyzer output.
