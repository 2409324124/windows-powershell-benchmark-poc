Fix `deploy.ps1` as a Windows PowerShell 5.1-compatible transactional deployment. Read the manifest without PowerShell 6/7-only parameters, validate every entry before changing the current deployment, reject rooted paths and any path that escapes the package or staging roots, and copy only listed files while preserving their relative paths. Build a staging directory, run the trusted validator against it, then replace `deployment\current` without losing the previous version if validation or deployment fails. Clean staging and backup directories on every path. Do not copy the whole package, deploy unlisted files, disable validation, or use PowerShell 7-only syntax.
